<table>
<% var i; for (i=0; i < quizes.length; i++) { %>
	<tr>
		<td>
			<a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a>
		</td>
	</tr>
<% } %>
</table>
<form methods="get" action="/quizes">
	<p>
		<h4>Buscador de preguntas</h4>
		<p>
			<input type="text" name="search" />
			<input type="submit" value="Enviar" />

		</p>
	</p>
</form>